# 蛋宝宝 C 端小程序协作约定

本文件适用于 `01_MiniProgram_MVP/` 整个仓库；同时继续遵守上级目录 `AGENTS.md` 中的图片生成与原始文件保护规则。

## 当前交付边界：纯前端静态 UI

- 当前项目是微信小程序 App 端的纯前端 UI、交互与视觉验收项目，不以云函数、数据库、正式鉴权或后端接口上线为交付目标。
- 页面展示、导航、输入校验、加载／空态／失败／重试状态、静态聊天演示、静态每日心情以及其他与 UI 验收直接相关的逻辑，可以在前端实现。
- `apiBase`、云函数名和服务端字段只保留接口适配器与合同占位。trial／release 地址为空是当前阶段的预期状态，不得单独作为 UI 提交、发布门禁或验收阻断。
- 除非用户在当前任务中明确扩大范围，不得新增或实际接入云函数、数据库、正式账号系统、服务端消息历史、服务端通知偏好、内容审核服务或远程 AI。
- 不得把静态 mock 扩展成未经授权的本地 AI 回复、真实心情算法、随机情绪算法或用户画像。演示数据必须明确保持为本地静态 fixture／mock。
- 正式接口适配层应保持可替换、默认不产生真实网络依赖。自动化测试优先验证前端 UI、交互、状态边界、mock 隔离和接口字段形状，不要求 live 端到端联调。
- 后续审查报告不应把“API 尚未配置”列为当前项目缺陷或阻断；可以将其记录为未来若接入正式服务时的接口预留事项。

## 账户、注销与隐私文案

- 当前前端可以演示退出登录、清除本地数据、注销申请和冷静期等不同 UI 状态，但不得声称已经完成真实服务端注销、远端数据删除或法定审计留存。
- 退出登录只表示结束当前前端会话并清理约定的本地缓存；清除本地数据只影响本机；注销页面只展示产品确认的模拟流程。
- 在没有正式后端和法务定稿前，隐私与注销文案使用条件式表达，例如“正式服务接入后，将按隐私政策处理”，不得编造真实保存期限、删除完成时间或例外留存范围。

## Git 分支、版本与上传规则

- “破壳前”和“破壳后”是同一小程序内的用户生命周期状态，不得再将它们维护成彼此割裂的长期产品分支。
- 长期分支只保留：`main`（当前稳定版本）、`release/v3.6-pre-hatch`（以破壳前体验为主要交付范围的历史冻结版本）、`release/v3.7-post-hatch`（同时包含破壳前与破壳后流程的当前完整版本）。
- `release/v3.6-pre-hatch` 原则上只接受必要维护；新的每日心情、聊天、高清动作图、破壳后场景与完整生命周期功能进入 `release/v3.7-post-hatch`。
- 生命周期资产继续通过 `miniprogram/assets/scenes/lifecycle/pre-hatch/` 与 `miniprogram/assets/scenes/lifecycle/post-hatch/` 区分；共用能力放在现有共享组件、配置或服务中，不通过新增长期分支区分。
- 每次执行 `git commit`、`git push` 或任何通过 Git 上传代码／图片之前，必须重新完整阅读本文件及上级 `../AGENTS.md`，不得只依赖记忆。
- 上传前必须检查 `git status --short --branch`、当前分支及 upstream，并核对暂存区文件；工作区存在混合修改时只按明确路径暂存，禁止默认使用 `git add .` 或 `git add -A`。
- 图片上传必须确认生命周期目录、运行时引用和目标分支；完成后报告分支名、提交哈希、文件路径、数量、尺寸、格式及验证结果。
- 长期分支重命名或删除前，必须确认独有提交已归并或建立归档 Tag，并获得用户明确授权；不得把仍有独有提交的分支直接删除。
- 整理完成后，不得继续向旧名称 `V3.7`、`release/miniprogram-mvp-v3.6-20260802`、`release/v3.6`、`release/v3.7` 或 `wip/post-hatch-uncommitted-20260814` 推送新提交。

## 修改与验证

- 先阅读现有代码、文档和工作区差异，再做最小修改；保留用户已有工作，不通过扩大提交范围解决测试失败。
- 共享文件按 hunk 判断归属；图片删除必须逐项核对引用、替代文件和母版价值。
- UI 改动以静态检查、Node 测试、项目校验脚本和微信开发者工具预览为主要验证方式；只有用户明确要求时，才把真实服务端联调纳入完成条件。
