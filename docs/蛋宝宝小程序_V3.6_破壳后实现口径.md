# 蛋宝宝小程序 V3.6 破壳后实现口径

## 文档优先级与范围

- 主产品事实来源为 `docs/蛋宝宝小程序_V3_6_PRD.md`。
- `docs/reference/蛋宝宝_日常陪伴系统_结构说明_v0.3.md` 补充空间和素材结构；两份文档冲突时以主 PRD 为准。
- 孵化前首页、一级入口、设备时钟、蛋壳绘图、许愿池和早教班继续沿用已确认实现，本轮不改变其布局与业务规则。

## 2026-08-14 · 当前实现快照

- 每日心情只实现前端静态 UI：固定配置支持且只支持愉快、平静、兴奋、好奇、关怀、焦虑、沮丧、疲惫 8 种展示状态，破壳前后共用组件与通用 Emoji；不接接口、数据库、日期、概率、用户行为或对话内容，不实现真实心情算法。
- 正式居家动作资源为玉兔 24 张 + 锦鲤 24 张：每个角色 8 个动作 × `day`、`sunset`、`night` 3 个时段，共 48 张 `2823×1672` 完整 WebP 全景。动作解析只读取角色、居家状态与时段，不读取季节、天气或复合环境键。
- 角色外出时不加载动作图，只按 3 个时段使用 3 张空房全景；破壳前的 36 个季节／天气／时段环境映射是独立资产体系，不与破壳后 48 + 3 张相加或套用。
- 聊天页与生活空间入口共用 `ui_3d_scene_chat_{jade_rabbit|boon_koi}_96_v02.png` 两张 96 PNG；`assets/ui/chat-avatars/*.webp` 当前无运行时引用。
- 当前 App 的外出实现只显示低饱和“外出中”状态，不展示地点、活动或中央叙事卡；这只是实现事实，不等于跨文档产品冲突已经确认。

> **待产品确认：** 主 PRD 与 Design 仍保留“空房 + 第一人称去向／状态说明”，《蛋宝宝 · 今日心情》专项稿与当前 App 则只显示“外出中”、不透露地点或活动。确认前不得仅凭本文改写主 PRD、服务端合同或审核文案。外出使用 3 张空房全景、不能聊天或触发角色动作、正常导航与设置不受影响的边界不变。

> **历史记录说明：** 下方从“历史实现记录”起均保留当时事实与验证结果，用于审计和恢复；其中 13 张玉兔动作、季节／天气动作匹配、外出左下留空等旧口径已被本节当前快照覆盖，不得继续作为开发或出图依据。

## 历史实现记录 · V3.6 破壳后落地

| 项目 | 当前口径 |
| --- | --- |
| 生活空间 | 固定左侧起居区、中间桌面、右侧布置区三屏连续空间；进入时定位到 ta 所在屏，用户可左右滑动查看空着的其他空间 |
| 此刻状态 | 以每只蛋的破壳时刻为起点，每 5 小时一个时段；同一时段刷新不改变 |
| 大场景 | 只使用家、旅行、打工、上学；用户不能手动选择；正式旅行目的地为大理、北京、西双版纳、东京 |
| 外出 | 三屏都显示空着的家，以第一人称文字说明去向；不提供对话、写信、留言或回信入口 |
| 对话 | 八个居家状态全部可对话；点击左下角角色按钮直接打开完整聊天页 |
| 回复等待 | 发送后自动滚到左侧三点气泡；三点以 `0.5s` 完整循环依次跳动，成功或失败即消失；弱动效显示静态三点 |
| 场景按钮 | 左下角角色对话图标只在居家时显示；外出时左下留空；右下角使用齿轮图标并直接进入我的 / 设置，不显示百宝箱弹层 |
| 对应动作 | 八个居家小场景各有一个审核过的固定动作；外出状态无用户动作与沟通入口 |
| 延后内容 | 收藏卡、明信片、纪念物与旅途回放保留代码和图片；V3.6 / V3.7 的 trial / release 无入口、无未读提示，旧路径转回“我的”；develop 可验收 |
| 日常窗外 | 使用应用内稳定天气、真实本机日间 / 日落 / 夜晚与第一人称陪伴文案；不读取天气 API、定位或上海天气 |
| 魔法窗外 | 独立功能只允许大理、北京、西双版纳三张正式景区素材；它与旅行目的地不是同一资产池，东京成为正式旅行目的地不自动进入魔法窗；三张正式素材未齐前关闭入口 |
| 右侧布置区 | 本轮只保留空间边界。主 PRD 未放行 AI 生成额度、装饰列表与拖放存储，因此不注册相关页面和接口 |

## 状态与数据边界

- demo 数据存放在账号隔离的 `eggbabe_post_hatch_v36` 命名空间，用于状态、居家动作、主动明信片与纪念物回归。
- `deferredContentEnabled` 在 V3.6 / V3.7 必须保持 `false`；只有 develop 环境可通过 `deferredContentDeveloperPreviewEnabled` 验收延后内容。页面代码与图片保留不等于发布开放。
- live 不由前端生成正式业务结果。环境视觉是例外：前端按稳定种子计算，不依赖天气 API；服务端只提供蛋身份字段和备案 CDN 根路径。
- live 端点为 `getPostHatchHome`、`performPostHatchAction`、`getPostHatchMemories`；不提供写信或回信接口。
- 服务端返回的小场景 ID 必须存在于固定场景表；前端以固定表覆盖动作、所在屏和说话权限，防止随机映射。
- `slot_end` 只用于到点重新请求，不展示倒计时或预告。

## 素材状态

所有运行时路径集中在 `miniprogram/config/post-hatch-assets.js`。破壳前中央裁图与破壳后三屏全景共享 36 个季节 × 天气 × 时段 key；每个 key 必须同时具备 `941×1672` 的预孵化 WebP 和 `2823×1672` 的破壳后全景 WebP。资源缺失直接显示加载失败与重试，禁止以其他天气场景兜底。项目目录保留源资产；正式运行由 bootstrap 的 `environment_cdn_base` 拼出备案 CDN 地址。

当前左右屏正式状态素材、完整角色姿态、明信片、心情头像以及三张正式魔法窗景区图仍未齐；页面保留清晰配置边界，不把参考合成图、草稿或第三方图片接入正式配置。东京已确认为正式旅行目的地，但现有东京图片仍是候选，须按《蛋宝宝旅行明信片内容与美术规格 v1.0》逐张终审并晋级后才能接入。已有透明单件纪念物会按明确 ID 复用。

## 2026-08-07 · 玉兔动作全景与东京旅途回放

- 玉兔卧室已接入睡觉、看书、打游戏、看窗外、画画、听音乐 6 个动作状态的晴天日间 / 夜间完整全景，并为睡觉状态增加完成关灯后的闭眼夜景，共 13 张。角色、道具、遮挡与阴影已烘焙，页面不再重复叠加透明角色、道具或环境色调层。
- `resolveActionPanorama` 只在玉兔、居家、对应状态、晴天且 `day` / `night` 条件全部匹配时返回动作全景；夜间睡觉完成关灯后重新解析并切换闭眼关灯版本，同时移除已完成动作的点击光效。日落、其他天气、其他角色或未配置状态继续使用对应环境全景，不静默复用错误光影。
- 居家固定状态由 7 个扩展为 9 个，新增“看书”和“听音乐”；两者均有固定文案、操作和对话权限。
- 破壳后主页跳转前使用不可见节点预热完整 Pano，然后无系统横向动画进入生活空间。生活空间首帧在背景和目标屏原生滚动位置均就绪后才淡入；运行中换图先预加载，失败时保留旧场景并提供重试。
- 东京旅行在明信片列表中聚合为一个“东京之旅”入口；点击详情大图进入独立旅途回放页，以完整横图模式浏览同次旅程。回放不导入实时动作、状态或业务存储模块，返回时保留所选明信片位置；旅程 ID、明信片必填字段与唯一 ID 均在展示前校验，避免坏数据导致顺序漂移。
- 动作全景目录从小程序主包排除，由 `environment_cdn_base` 运行时加载。东京候选图为保留开发验收暂未在 `packOptions.ignore` 排除；发布 trial / release 前必须处理候选资产晋级或专用打包配置，并复核上传文件清单。
- 当前未闭环：动作全景尚缺日落和非晴天版本；东京 12 张 v02 仍是开发验收候选，未进入正式 `POST_HATCH.postcards`。

验证结果：`journey-scene.test.js`、`memory-demo-preview.test.js`、`post-hatch-panel-scenes.test.js`、`post-hatch-companion.test.js`、`release-gate.test.js`、`demo-pages.test.js`、`verify-project.js`、`verify-v2.js`、`verify-package-assets.js` 均通过；工程现为 23 个页面，包体静态门禁通过。

### 2026-08-06 · 静态 3D 玉兔 MVP 停止，恢复 2D 角色图

- 玉兔“在家、静态坐姿、单一 Pano”的 Three.js MVP 已验证可加载，但材质、光照、比例、朝向、接触阴影与预生成全景无法稳定融合；本版本不继续投入 3D 调参。
- 运行时关闭并移除 GLB、WebGL 组件和 Three.js 小程序依赖；原始输入、模型、组件和验证记录保留在 `docs/archive/2026-08-06-static-3d-jade-rabbit-mvp/`，不参与构建和包体。
- 透明 WebP 角色叠加样张已完成验证，但与预生成 Pano 的暖色、透视、接触阴影、遮挡和道具关系不能稳定融合，不作为正式美术方案继续扩展。
- 后续角色与场景必须一起生成整张 `2823 × 1672` 三屏连续 Pano：以对应空房间 Pano 锁定构图，角色、固定动作、光照、投影和遮挡一次烘焙。运行时只切换审核通过的完整 Pano，不再把透明角色图跨环境复用。
- 首次只验证“玉兔 + 在家发呆 + 中屏圆垫 + 指定日落 Pano”1 张。通过后再按 7 个居家状态 × 3 个时段逐步扩展；外出继续显示空着的家，不生成三屏外出角色图。旅行中的角色与目的地可烘焙在独立明信片插画中，回家后送达。

## 2026-08-05 · 回忆页视觉统一与开发预览状态

> 本节记录的是已保留的 develop 验收实现。按 2026-08-09 发布范围收缩，V3.6 / V3.7 的 trial / release 不显示这些页面或入口。

### 页面视觉调整

- `pages/life-scenes/life-scenes.wxss` 已按 `app.wxss` 中的 EggBabe Design System 统一：主色使用 `#002900`，一级文字使用 `#1A1A1A`，二级文字使用 `#5C5C5C`，三级文字使用 `#8C8C88`，分隔线使用 `#E5E3DF`；页面不再使用旧版近似绿色和灰色。
- 百宝箱中的“ta 带回来的东西”和“明信片”分别从各自入口直接展示对应内容，顶部不再重复显示栏目切换；纪念物详情的来历文字显示为“来自：场景 · 动作”，使用 `24rpx / 600`，故事正文为 `30rpx / 400`。页面字重最高为 600。
- 有内容时，纪念物采用三列缩略图列表；只显示已经出现的物件，点击后进入单件大图与故事详情。不设置总数、锁定格、空位、完成度或图鉴提示。
- 纪念物详情将物件图作为首屏唯一视觉焦点，主图容器收紧为 `560rpx`；来历下方使用“我还记得”第一人称回忆区，采用 Design System 桃粉霜 tint，不新增按钮、进度或获取状态。
- 明信片采用封面列表，列表显示主题与日期，点击后进入封面与正文详情。正式明信片场景图尚未接入时，使用原生纸张视觉，不使用参考图或第三方图片作为静默占位。
- 纪念物三列小卡与明信片列表封面在按压时共用原生 `hover-class` 波光：卡片以 `180ms` 轻抬，白金色高光以 `400ms` 扫过。不使用彩虹稀有卡语义、高频 `touchmove` 或第三方 DOM 动画库；减少动态效果时退化为静态亮度反馈。
- 今日收藏卡改为显示真实收藏卡缩略卡，包括固定角色插画、名称与款式；点击仍进入原收藏卡详情页。

### 仅开发版可见的预览切换

- develop 回忆页右下角保留一个 `112rpx` 圆形图形按钮，点击循环切换三组页面数据，用于检查延后内容的空态、少量内容布局和较长列表布局；trial / release 不可进入该页面。
- 按钮只使用三个无文字圆点表达状态，不在页面中显示协作时使用的状态名称，也不弹出对应文案。
- 按钮严格使用 `config.localDemoEnabled` 门禁，只在微信开发版渲染；trial 与 release 均不会显示。
- 预览数据由 `utils/memory-demo-preview.js` 纯函数生成，不调用后端、不写 storage，也不改变 `post-hatch-companion` 返回的真实数据。
- 三组数据依次覆盖：三个分页均为空；3 件纪念物、2 张文字明信片和当前收藏卡；当前配置中 10 件带正式运行时图片的纪念物、1 个“东京之旅”入口（内部含咖啡巷与城市露台共 12 张候选图）和当前收藏卡。以上数量只存在于开发夹具和本实现记录，不在用户页面显示。
- 东京旅程详情中的大图是旅途回放入口；点击后进入独立全屏场景，在同一次旅程中横向浏览。明信片详情不显示胶囊形滑动假按钮。
- 旅途回放只更新当前图片索引，不调用动作、场景或状态接口，不改变 ta 当前所在状态；返回时保留进入前所选的明信片位置。
- 回放页在等待事件数据时显示加载状态；无旅程、旅程 ID 不匹配、明信片字段不完整或图片加载失败时显示可返回的明确提示，不以空白或其他目的地图片兜底；顶部导航、底部说明与错误态适配横竖屏安全区。
- 候选图片仍必须留在开发验收路径。当前小程序的 `packOptions.ignore` 同时影响“预览”和“上传”，不能在不破坏微信开发版验收的前提下只把候选图从 trial / release 包中排除；现阶段仅验证其不进入正式运行时配置且 trial / release 不渲染预览控件，发布前仍需在开发者工具的上传文件清单做人工包体复核。
- 今日收藏卡仍遵循“一天推荐一张”的产品语义；第三组预览不会改造成多卡图鉴。

### 变更文件

- `miniprogram/pages/life-scenes/life-scenes.wxml`
- `miniprogram/pages/life-scenes/life-scenes.wxss`
- `miniprogram/pages/life-scenes/life-scenes.js`
- `miniprogram/pages/journey-scene/*`
- `miniprogram/utils/memory-demo-preview.js`
- `miniprogram/services/tests/journey-scene.test.js`
- `miniprogram/services/tests/memory-demo-preview.test.js`

### 验证结果

- `node miniprogram/services/tests/journey-scene.test.js`：通过，覆盖入口传参、原索引进入、本地滑动、状态隔离、事件等待和异常恢复。
- `node miniprogram/services/tests/memory-demo-preview.test.js`：通过。
- `node miniprogram/services/tests/post-hatch-companion.test.js`：通过。
- `node miniprogram/services/tests/release-gate.test.js`：通过。
- `node scripts/verify-project.js`：通过，23 个页面及关键 PRD 约束正常。
- `node scripts/verify-v2.js`：通过，V3.6 普通小程序核心合规回归正常。
- `node --check`：回忆页逻辑和预览夹具语法检查通过。
- 微信开发者工具 CLI 预览未完成：本机 IDE 的服务端口处于关闭状态，CLI 在初始化阶段退出；未擅自更改该安全设置。
- `node scripts/verify-package-assets.js`：通过，已校验运行时资源、CDN 外部化目录与 20 MiB 静态门禁。

## 2026-08-05 · 恢复破壳后阶段验收入口

- 破壳后生活场景右上角重新显示开发版阶段验收器，位置在“场景”与“状态”两个验收器之间。
- 入口继续复用既有 Day 1–Day 7 / 破壳后数据、切换逻辑和样式；选择孵化阶段后返回首页，选择破壳后则留在当前生活场景并刷新状态。
- 入口同时受 `isDemo`、`currentState` 与 `sceneBackgroundReady` 保护，只在微信开发版且场景全景加载成功后出现，trial / release 不渲染。
- 回归测试已改为要求入口存在、可展开选择，并保持三项验收器的纵向顺序。

## 2026-08-05 · 百宝箱回忆层级与返回恢复

> 本节为历史实现记录。百宝箱弹层已从 V3.6 / V3.7 正式界面移除；下列页面代码与图片仅在 develop 环境保留验收。

- “ta 带回来的东西”按确认稿改为三列缩略图浏览，点击任一已出现物件后，通过独立页面栈进入单件大图与故事详情；左上角返回先回到纪念物列表。
- 参考稿中的“已解锁 / 未解锁”仅作为排布示意，运行页面不显示锁定状态、灰色空位、数量或完成进度。
- 明信片改为纵向封面列表，显示主题和日期；点击后通过独立页面栈进入明信片封面与正文详情。
- 正式明信片插画为空时使用暖白复古纸张占位，由小程序原生层绘制，不接入参考图或第三方素材。旅行明信片插画只在角色回家后通过列表与详情展示，不用于旅行期间的三屏生活空间。
- 原百宝箱返回恢复逻辑已退出 V3.6 / V3.7 正式界面；对应内容页与内部导航代码保留在 develop 环境，旧正式路径统一转回“我的”。
