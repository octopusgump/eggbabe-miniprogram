# 孵化交互特效素材

v2.23 前端已用 CSS 实现雨、雪、云和轻点粒子，素材缺失时仍可完整运行。后续如以品牌定制 PNG 替换，文件名固定为：

- `fx_rain.png`
- `fx_snow.png`
- `fx_cloud.png`
- `fx_star.png`
- `fx_confetti.png`

雨丝、雪花与星点建议不超过 `64×64`，云图建议不超过 400px 宽。特效只负责视觉，不参与业务判定。
