# 破壳后 · 左屏起居区

正式素材统一放入 `scene-sets/`，命名为 `{scene_key}_left_living.webp`。
完整画布、切图、状态和验收规则见 `../THREE_PANEL_SCENE_SET_SPEC.md`。

当前破壳前配置共有 20 个有效 scene key，因此左屏待生成 20 张。必须以对应中屏原图为锚点向左扩图，不得把三屏分别独立生成。

可合并墙面、床架、固定枕头、静态床品和后景家具；不含角色、文字、台灯暗光、被子盖前/盖后状态及其他交互反馈。
