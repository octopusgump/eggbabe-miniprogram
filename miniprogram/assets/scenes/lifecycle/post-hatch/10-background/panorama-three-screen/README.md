# 破壳后 · 连续全景运行素材

`scene-sets/` 内保留 36 张正式运行图，每张均为 `2823 × 1672` WebP，文件名为
`{scene_key}_post_hatch_panorama_v01.webp`。

页面以一张图覆盖三屏横向轨道；左、中、右位置只承担滚动定位与互动热区，不再分别加载或裁切背景图。这样可以避免不同设备上独立 `aspectFill` 裁切造成的拼接线。

全景图不含角色、蛋、文字和热点；这些内容由页面独立叠加。
