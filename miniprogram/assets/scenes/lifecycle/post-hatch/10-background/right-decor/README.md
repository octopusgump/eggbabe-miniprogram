# 破壳后 · 右屏布置区

正式素材统一放入 `scene-sets/`，命名为 `{scene_key}_right_decor.webp`。
完整画布、切图、状态和验收规则见 `../THREE_PANEL_SCENE_SET_SPEC.md`。

当前破壳前配置共有 20 个有效 scene key，因此右屏待生成 20 张。必须以对应中屏原图为锚点向右扩图，不得把三屏分别独立生成。

可合并墙面、地面、窗框、匹配当前状态的静态上海窗景、边几和固定后景；不含角色、天气粒子、用户家具、魔法窗景区、文字和热点。
