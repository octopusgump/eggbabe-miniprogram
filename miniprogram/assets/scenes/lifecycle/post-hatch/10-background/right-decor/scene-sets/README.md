# 右屏正式状态素材投放目录

这里放置 `{scene_key}_right_decor.webp`。完整 20 状态清单和验收规则见 `../../THREE_PANEL_SCENE_SET_SPEC.md`。

文件放入目录后不会自动上线；左右两屏同时验收通过后，还需在 `miniprogram/config/post-hatch-assets.js` 的 `READY_PANEL_SCENE_KEYS` 中加入对应 key。
