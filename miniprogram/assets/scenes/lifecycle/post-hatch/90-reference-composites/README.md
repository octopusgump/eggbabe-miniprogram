# 破壳后 · 合成构图参考

这里仅保存仍被生成文档引用的构图参考与无损 PNG 源图，不作为正式运行时素材。

当前保留 `center-desk` 构图参考，以及 `originals/` 中的 `draw_day`、`music_day`、`window_day` 三张结构参考。取消的季节／天气矩阵和未引用原稿已于 2026-08-12 移至项目外 `60-action-scenes-backup/cleanup-2026-08-12/`。

已审核的玉兔与锦鲤卧室动作全景运行时版本位于 `../60-action-scenes/`，动作解析器只按状态与 `day`、`sunset`、`night` 三个时段加载；不得回写或混入本目录。
