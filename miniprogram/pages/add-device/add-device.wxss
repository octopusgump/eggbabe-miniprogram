.page { display: flex; flex-direction: column; height: 100vh; background: #FAFAF8; }
.content { flex: 1; display: flex; flex-direction: column; padding: 48rpx; box-sizing: border-box; }

.intro { margin-bottom: 52rpx; }
.intro-title { display: block; font-size: 40rpx; font-weight: 600; color: #1A1A1A; margin-bottom: 16rpx; }
.intro-desc { display: block; font-size: 26rpx; color: #5C5C5C; line-height: 1.7; }

.code-block { margin-bottom: 40rpx; }
.label { display: block; font-size: 24rpx; color: #8C8C88; margin-bottom: 16rpx; }
.activation-code { position: relative; width: 100%; }
.activation-code-cells { display: flex; justify-content: space-between; gap: 14rpx; }
.activation-code-cell {
  flex: 1; height: 108rpx; border-radius: 18rpx; border: 2rpx solid transparent;
  background: #EEF1F6; display: flex; align-items: center; justify-content: center;
  box-sizing: border-box; color: #1A1A1A; font-family: "SF Mono", "Menlo", monospace;
  font-size: 48rpx; font-weight: 600; line-height: 1; transition: border-color 160ms ease, background 160ms ease, box-shadow 160ms ease;
}
.activation-code-cell--filled { background: #FFFFFF; border-color: #D8D6D0; }
.activation-code-cell--active { border-color: #17472B; box-shadow: 0 0 0 5rpx rgba(23,71,43,0.10); }
.activation-code-native-input {
  position: absolute; inset: 0; width: 100%; height: 108rpx; opacity: 0.01;
  color: transparent; caret-color: transparent; font-size: 1rpx;
}
.code-hint { display: block; margin-top: 18rpx; font-size: 22rpx; color: #9B9993; }

.error { font-size: 24rpx; color: #D9463C; margin-bottom: 24rpx; }
.spacer { flex: 1; }

/* 成功浮层 */
.success-mask {
  position: fixed; inset: 0; background: rgba(250,250,248,0.97);
  display: flex; flex-direction: column; align-items: center; justify-content: center; z-index: 80;
}
.success-egg-stage { position: relative; width: 260rpx; height: 260rpx; display: flex; align-items: center; justify-content: center; margin-bottom: 40rpx; }
.success-glow {
  position: absolute; width: 220rpx; height: 220rpx; border-radius: 50%;
  background: radial-gradient(circle, rgba(237,231,142,0.6) 0%, rgba(255,255,255,0) 70%);
  animation: glowPulse 1.2s ease-in-out infinite;
}
.success-egg {
  position: relative; width: 160rpx; height: 188rpx; border-radius: 50% 50% 50% 50% / 58% 58% 42% 42%;
  border: 4rpx solid #FFFFFF; box-shadow: 0 8rpx 24rpx rgba(0,41,0,0.10);
  background: linear-gradient(160deg, #EDE78E, #F4B9AE);
  animation: popIn 0.4s cubic-bezier(0.34,1.56,0.64,1);
}
.success-text { font-size: 32rpx; font-weight: 600; color: #1A1A1A; }
@keyframes glowPulse { 0%, 100% { opacity: 0.55; transform: scale(1); } 50% { opacity: 0.9; transform: scale(1.15); } }
@keyframes popIn { 0% { transform: scale(0); opacity: 0; } 100% { transform: scale(1); opacity: 1; } }
@media (prefers-reduced-motion: reduce) { .success-glow, .success-egg { animation: none; } }
