<view class="page"><nav-bar title="我的激活码"></nav-bar><scroll-view class="content" scroll-y><view class="intro"><text class="title">把一颗实体蛋带给朋友</text><text class="desc">可用的个人激活码由服务端在成功绑定后创建。每个码只能完成一次实体蛋绑定。</text></view><view wx:if="{{codes.length}}" class="code-list"><view wx:for="{{codes}}" wx:key="code" class="code-row {{item.used ? 'code-row--used' : ''}}"><view><text class="code">{{item.code}}</text><text class="status">{{item.used ? '已使用' : '可使用'}}</text></view><button class="copy" hover-class="button-press" disabled="{{item.used}}" data-code="{{item.code}}" bindtap="onCopy"><text class="button-label">复制</text></button></view></view><view wx:else class="empty">暂无可用的个人激活码。</view><view class="note">个人激活码只用于来源追溯和实体蛋绑定，不附带其他权益。</view></scroll-view></view>
