<view class="page"><nav-bar title="我的激活码"></nav-bar><scroll-view class="content" scroll-y><view class="intro"><text class="title">把一颗蛋送给朋友</text><text class="desc">成功添加第一只蛋宝宝后，你会拥有 5 个一次性个人激活码。每个码只能被一位朋友使用。</text></view><view wx:if="{{codes.length}}" class="code-list"><view wx:for="{{codes}}" wx:key="code" class="code-row {{item.used ? 'code-row--used' : ''}}"><view><text class="code">{{item.code}}</text><text class="status">{{item.used ? '已使用' : '待邀请'}}</text></view><button class="copy" disabled="{{item.used}}" data-code="{{item.code}}" bindtap="onCopy"><text class="button-label">复制</text></button></view></view><view wx:else class="empty">添加第一只蛋宝宝后，激活码会出现在这里。</view><view class="note">个人激活码不提供积分、排行榜或现金奖励。只有朋友成功绑定蛋宝宝后，激活码才会变为已使用。</view></scroll-view></view>
