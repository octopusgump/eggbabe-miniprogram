<view class="page">
  <nav-bar title="我的" fallback-url="/pages/home/home"></nav-bar>

  <view class="content">

    <view class="user-card" hover-class="user-card--pressed" bindtap="onTapUserCard">
      <egg-avatar size="{{56}}" gradient-from="#9DB65B" gradient-to="#EDE78E"></egg-avatar>
      <view class="user-info">
        <text class="user-name">{{userName}}</text>
        <text class="user-sub">{{eggCount}} 只蛋宝宝</text>
      </view>
      <view class="chevron"></view>
    </view>

    <view class="menu-panel">
      <list-row label="我的收藏卡" bind:rowtap="onNavAlbum"></list-row>
      <list-row label="我的激活码" bind:rowtap="onNavCodes"></list-row>
      <list-row label="对话记录" bind:rowtap="onNavChatRecords"></list-row>
      <list-row label="系统设置" bind:rowtap="onNavSettings"></list-row>
      <list-row label="帮助中心" show-border="{{false}}" bind:rowtap="onNavHelp"></list-row>
    </view>

  </view>
</view>
