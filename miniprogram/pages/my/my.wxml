<view class="page">
  <nav-bar title="我的" show-back="{{false}}"></nav-bar>

  <view class="content">

    <view class="user-card" hover-class="user-card--pressed" bindtap="onTapUserCard">
      <egg-avatar size="{{56}}" gradient-from="#9DB65B" gradient-to="#EDE78E"></egg-avatar>
      <view class="user-info">
        <text class="user-name">{{userName}}</text>
        <text class="user-sub">{{eggCount}} 只蛋宝宝</text>
      </view>
      <view class="chevron"></view>
    </view>

    <view class="pet-status" bindtap="onPetAction" hover-class="user-card--pressed">
      <view class="pet-visual {{pet ? '' : 'pet-visual--empty'}}"><text wx:if="{{pet}}">{{pet.prototype === '锦鲤' ? '锦' : '兔'}}</text></view>
      <view class="pet-copy">
        <text class="pet-title">{{pet ? (pet.name || pet.prototype) : '还没有蛋宝宝'}}</text>
        <text class="pet-stage">{{pet ? stageLabel : '输入激活码，领取你的第一只蛋宝宝'}}</text>
        <text wx:if="{{pet}}" class="pet-line">{{statusLine}}</text>
      </view>
      <text class="pet-action">{{pet ? actionLabel : '添加蛋宝宝'}}</text>
    </view>

    <view class="menu-panel">
      <list-row label="我的卡册" bind:rowtap="onNavAlbum"></list-row>
      <list-row label="我的激活码" bind:rowtap="onNavCodes"></list-row>
      <list-row label="系统设置" bind:rowtap="onNavSettings"></list-row>
      <list-row label="账号" bind:rowtap="onNavAccount"></list-row>
      <list-row label="隐私协议" bind:rowtap="onNavPrivacy"></list-row>
      <list-row label="帮助中心" show-border="{{false}}" bind:rowtap="onNavHelp"></list-row>
    </view>

  </view>
</view>
